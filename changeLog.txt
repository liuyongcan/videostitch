2018-8-15：
1.有些图像与基准图像的匹配点很少，无法准确得到单应矩阵，拼接的图片并不理想
2.整理以前代码，直接使用简单粗暴的方法一张一张的拼接，一张新的图像就与之前拼接的图像进行特征点选取和特征点匹配，
  拼接效果比之前要好一些，只是需要频繁寻找和匹配特征点。

2018-8-14：
1.在网上看到了ORB特征提取和特征匹配算法，进过一次特征点筛选再利用findHomography函数得出的变换矩阵。
2.将原图像大小变大但图像不放大，可以得到变换后的完整图像。
3.利用变换矩阵进行两张图片的投影，已可以完成基本重合。

2018-8-13：
1.利用最大生成树算法找到中心节点，以此节点为基准图像，将其他图像投影到它上获得全景图，但拼接有偏差。

2018-8-9：
1.利用单应性矩阵完成第一张投影到第二图，但还有些对不齐
2.自己完成还是有点困难，希望能够学习hugin源码完成

2018-8-7:
1.按帧间隔获取到图片
2.获取到匹配信息